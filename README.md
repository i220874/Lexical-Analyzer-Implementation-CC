# Lexical-Analyzer-Implementation-CC
implement a Lexical Analyzer (Scanner) for a custom programming language. The lexical analyzer is the first phase of a compiler that reads source code and breaks it into meaningful units called tokens.

